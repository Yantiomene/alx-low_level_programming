#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: string to return the lenght
 *
 * Return: the lenght of s
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
