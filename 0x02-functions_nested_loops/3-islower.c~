#include "main.h"

/**
 * _islower - check if a character is lowercase
 *
 * Return: 1 (lowercase) 0(otherwise)
 */
int _islower(int c)
{
	int res;

	if ((97 <= c) && (122 >= c))
	{
		res = 1;
	} else
	{
		res = 0;
	}
	return (res);
}
